This repository contains `noted`, a small Python command-line tool for adding and listing short notes, with `noted.py` as the main entry point, JSON-backed storage at `~/.noted.json` unless `NOTED_DB` overrides the path, and `pytest` coverage for the basic add-and-list workflow in `test_noted.py`.
