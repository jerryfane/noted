# clear

Clear all notes.

    python noted.py clear --yes

Without `--yes`, prints `pass --yes to clear notes` and leaves notes unchanged.
With `--yes`, removes all notes and prints `cleared <count> notes`.
