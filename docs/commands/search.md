# search

Search notes for a term.

    python noted.py search milk

Matches are case-insensitive and are printed as `#<id>\t<text>`. Prints
`no matching notes` when no stored note contains the term.
