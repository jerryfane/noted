# dedupe

Remove duplicate notes with identical text, keeping the first occurrence in
stored order.

    python noted.py dedupe

Prints the number of removed duplicate notes, e.g. `removed 1 duplicates`.
When there are no duplicates, prints `removed 0 duplicates`.
