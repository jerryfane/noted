# longest

Print the note with the longest text.

    python noted.py longest

Prints the note as `#<id>\t<text>`, or `no notes yet` when empty. If multiple
notes have the same longest text length, prints the first one in stored order.
