# tail

Print the most recent notes.

    python noted.py tail
    python noted.py tail --limit 10

Prints the last 5 notes by default. Use `--limit N` to print the last N notes.
Each note uses `#<id>\t<text>`, or `no notes yet` when empty.
